{
  "name": "infection-server",
  "version": "0.0.1",
  "description": "The WebSockets server for Infection MP",
  "main": "index.js",
  "scripts": {
    "dev": "npx tsc && nodemon src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jamie Rossiter",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^4.6.1",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.4",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
